/* eslint-disable @typescript-eslint/no-explicit-any */
import AdminLayout from '@/Layouts/AdminLayout'; import { Head } from '@inertiajs/react'
export default function History({periods}:{periods:{data:any[]}}){return <AdminLayout title="Agent status history"><Head title="Status history"/><div className="rounded-3xl border bg-white p-6"><div className="space-y-3">{periods.data.map(p=><div key={p.id} className="rounded-2xl border p-4"><div className="font-medium">{p.status?.name}</div><div className="text-sm text-gray-500">{p.scope}{p.channel?` · ${p.channel}`:''} · {new Date(p.started_at).toLocaleString()} — {p.ended_at?new Date(p.ended_at).toLocaleString():'Current'} · {p.source}{p.end_reason?` · ${p.end_reason}`:''}</div>{p.note&&<p className="mt-2 text-sm">{p.note}</p>}</div>)}</div></div></AdminLayout>}
